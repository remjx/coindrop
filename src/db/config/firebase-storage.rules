rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    match /images/users/{ownerUid}/coindrops/{piggybankName}/avatar {
      allow delete: if request.auth.uid == ownerUid;
    }
  }
}
